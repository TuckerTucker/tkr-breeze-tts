# Conditional rules

No repository-local conditional Claude rules are installed. Add a rule here only when the
repository needs guidance that applies to a mechanically identifiable subset of work; document
the supported selector in that rule rather than copying assumptions from another project.
